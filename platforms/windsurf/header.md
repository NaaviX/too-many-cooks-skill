# Too Many Cooks — Crypto Funding Rates

Windsurf rule for funding-rate / delta-neutral arbitrage queries. Requires the `@toomanycooks/mcp-server` MCP server (config in the accompanying `mcp-snippet.json`).
